{
  "name": "Juniper Sales CRM",
  "install": "bash .cursor/install.sh",
  "start": "bash .cursor/start.sh",
  "terminals": [
    {
      "name": "API (FastAPI :8000)",
      "description": "FastAPI backend served against the local MariaDB.",
      "command": "bash .cursor/start.sh && set -a && . .cursor/dev.env && set +a && . .venv/bin/activate && uvicorn api.server:app --host 0.0.0.0 --port 8000 --reload"
    },
    {
      "name": "Studio (Vite :5174)",
      "description": "React/Vite dev server; runs against MSW mocks (VITE_MOCK=true), no backend required.",
      "command": "cd studio && VITE_MOCK=true npm run dev -- --host 0.0.0.0"
    }
  ],
  "ports": [
    { "name": "api", "port": 8000 },
    { "name": "studio", "port": 5174 }
  ]
}
