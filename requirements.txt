fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.0
bcrypt>=4.0.0
msal>=1.28.0
PyJWT>=2.8.0
python-dotenv==1.0.1
httpx>=0.27.0,<0.29.0
msgraph-sdk>=1.0.0
azure-identity>=1.15.0
google-cloud-kms>=2.21
google-cloud-storage>=2.14
aiomysql>=0.2.0
playwright>=1.47,<2.0
python-multipart>=0.0.9
# Handoff 47: append the estimate's proposal documents (contract, measurements,
# other) to the tail of the rendered PDF. pypdf merges + counts pages; Pillow
# wraps uploaded images in a Letter page. Both are pure wheels (no system
# packages). Already installed in venv/ but were absent here, so the Docker
# image shipped without them.
pypdf>=6.0,<7
Pillow>=11,<13
